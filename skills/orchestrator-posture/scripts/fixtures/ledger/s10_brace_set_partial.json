{
  "description": "S10: a brace set is satisfied only when EVERY member exists; a partial set is missing, and the absent members are named",
  "expect": {
    "deliverables.value.missing": 1,
    "deliverables.value.present": 0
  },
  "expect_failure_codes": [
    "lost-deliverable"
  ],
  "files": {
    "scripts/a.sh": "x\n",
    "scripts/b.sh": "x\n"
  },
  "manifest_md": "- `scripts/{a,b,c}.sh` \u2014 check: `true`\n",
  "sentinel": {
    "complete": true
  }
}
